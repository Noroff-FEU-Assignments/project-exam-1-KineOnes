import { contactPage } from "./pages/contactPage.js";
import { homepge } from "./pages/contactPage.js"

// We have to make sure that the right code fires on the right page

// Router

if (window.location.pathname === "home.html") {
    homePage()
}